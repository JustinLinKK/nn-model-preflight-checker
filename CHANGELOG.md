# Changelog

## 0.1.0 - Unreleased

- Add the standalone PyTorch preflight controller, isolated worker protocol, schemas, CLI,
  static checks, runtime checks, hardware profiles, and transparent memory-risk estimate.

